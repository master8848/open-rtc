{
  "name": "@mbsks/openrtc-protocol",
  "version": "0.1.0",
  "description": "vidcall wire protocol types (TS mirror of protocol/schema.json — the single source of truth).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/types.js",
  "types": "./dist/types.d.ts",
  "exports": {
    ".": {
      "types": "./dist/types.d.ts",
      "import": "./dist/types.js",
      "require": "./dist/types.cjs",
      "development": "./types.ts",
      "default": "./dist/types.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "tsc -b --clean",
    "build:cjs": "tsc -p tsconfig.cjs.json",
    "build:dual": "tsc -p tsconfig.json && npm run build:cjs && node ../scripts/post-build-cjs.mjs 2>/dev/null || echo \"CJS .js -> .cjs rename pending (rslib produces .cjs natively)\""
  }
}

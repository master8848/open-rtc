# Agent Note: Roadmap to 1.0

Status: implemented

Rationale: additive milestones needed ordering and acceptance gates with breaking-change policy to 1.0.

Files: `plans/07-roadmap.md` → `docs/plans/archive/07-roadmap.md`; operator surface in `packages/server/README.md` + `docs/limits.md` + `docs/testing.md` matrix + `CHANGELOG.md` history.

Decisions: M0 foundations Relay+packaging+token, M1 SFU MediaTransport wiring, M2 secure+recording product, M3 resilience composite/reconnect, M4 DX polish, M5 advanced one PR per feature; pre-1.0 additive with deprecation shims, types src→dist once, Envelope v frozen at 1.0 with additive types; L0 fixtures + quicktype gate, publint/attw/size-limit before publish, Rust sidecar only after JS relay benchmark.

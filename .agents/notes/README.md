# Notes — rationale

Rationale lives here per `docs/AGENTS.md`. Current behavior → `docs/` reference pages; operator env vars → `packages/server/README.md` and `docs/limits.md`; standing rules → `docs/AGENTS.md`. Elsewhere link, do not duplicate.

Format: `# Agent Note: <title>` blank line `Status: implemented` then rationale, files, decisions. One physical line per paragraph. Concrete file anchors, no history narration.

Classes: `security`, `recording`, `media-topology`, `transport`, `packaging`, `advanced-media`, `overview`, `roadmap` map to `plans/00-07`. Word budget for this file ≤500.

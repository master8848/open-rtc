# v0.2.0 Roadmap discussion stub (Query #4252 style)

> Open as GitHub Discussion: `v0.2.0 Roadmap` (category: Roadmap) — alphas on `next` dist-tag, 16 RCs pattern (TanStack), `codemod/room-config-rename.mjs` for `RoomConfig` renames, `changeset version --dry-run` gate (`plans/07-roadmap.md:77`).

## Proposed scope (additive, 1 PR/feature)

- **Moderation/lobby** — `room.moderate({kick,mute,lock,ban})`, lobby queue `POST /rooms/:id/lobby/admit`, poll/hand queue server-persisted.
- **Transcription/captions** — client `media/transcription.ts` + server `SfuTranscriptionWorker` → `transcript` envelope (`protocol/schema.json`).
- **Recording product** — `SfuGateway.egress` → `EgressWorker PlainTransport→ffmpeg`, `manifest.encrypted/keyId`, `breakout` assignment ids.
- **Quality train** — `test:types .test-d.tsx` multi-TS 5.5-5.9, `publint+attw`, `size-limit` `core <15kB gz`, `sherif/knip/zizmor` stubs land in `ci.yml`.

## Release cadence

- Weekly changelog publish to `CHANGELOG.md` + GitHub Releases; `protocol v` bump-only-on-break (`protocol/schema.json:5`); feature flags via `protocol v`.
- `fixed` groups `.changeset/config.json:7`; `changeset version --dry-run` in CI; migration guide `docs/guides/migration.md`.
- `Quality Wednesdays` habit — `quality` label papercuts tracked weekly.

## Links

`plans/07-roadmap.md`, `plans/08-coherence-dx-docs-plan.md:111`, `protocol/schema.json:5`, `docs/testing.md` CI matrix.
